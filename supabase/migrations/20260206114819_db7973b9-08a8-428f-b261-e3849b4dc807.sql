-- Drop knowledge_base table (no longer needed)
DROP TABLE IF EXISTS public.knowledge_base;

-- Drop department_heads table (no longer needed - was for routing approvals)
DROP TABLE IF EXISTS public.department_heads;
